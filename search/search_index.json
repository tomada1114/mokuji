{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"mokuji","text":"<p>The most readable way to browse Markdown in your terminal.</p> <p>mokuji (\u76ee\u6b21, \"table of contents\") is a terminal Markdown reader built with Textual. Run it in any repository and read its documentation with Vim-style keys, a polished dark theme, and an always-visible key guide.</p>"},{"location":"#installation","title":"Installation","text":"uvpipxpip <pre><code>uv tool install mokuji\n</code></pre> <pre><code>pipx install mokuji\n</code></pre> <pre><code>pip install mokuji\n</code></pre>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>cd your-project\nmokuji                # browse the repository\nmokuji docs/usage.md  # open one file directly\n</code></pre> <p>Press <code>?</code> inside the app for the full key reference.</p>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Getting Started \u2014 setup and first steps</li> <li>Reference \u2014 CLI options and every keybinding</li> <li>Contributing \u2014 how to contribute</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>Thank you for considering a contribution! This document explains how to set up your development environment and submit changes.</p>"},{"location":"contributing/#prerequisites","title":"Prerequisites","text":"<p>Install these tools:</p> <ul> <li>Python 3.12+</li> <li>uv</li> <li>Just (optional \u2014 you can run   <code>uv run</code> commands directly)</li> </ul> <p>Then:</p> <pre><code>uv sync --all-groups\n</code></pre> <p>If you're working in a Git checkout, also install the local hooks:</p> <pre><code>uv run pre-commit install --install-hooks\n</code></pre>"},{"location":"contributing/#development-workflow","title":"Development Workflow","text":"<pre><code># Format and auto-fix\njust fmt\n\n# Lint + type check\njust lint\n\n# Run tests\njust test\n\n# Build and verify the wheel in an isolated temp environment\njust smoke\n\n# Run everything (format \u2192 lint \u2192 test)\njust check\n</code></pre> <p>Without Just, run the equivalent commands:</p> <pre><code>uv run ruff format .\nuv run ruff check --fix .\nuv run ruff check .\nuv run mypy src scripts tests\nuv run pytest --cov=mokuji --cov-branch --cov-report=term-missing:skip-covered --cov-fail-under=80\nuv build &amp;&amp; uv run python scripts/smoke_test.py\n</code></pre>"},{"location":"contributing/#pull-request-process","title":"Pull Request Process","text":"<ol> <li>Fork the repository and create a branch from <code>main</code></li> <li>Make your changes</li> <li>Ensure <code>just check</code> passes</li> <li>Write or update tests for your changes</li> <li>Open a pull request using the PR template</li> </ol>"},{"location":"contributing/#code-standards","title":"Code Standards","text":"<ul> <li>All public functions and methods must have type annotations</li> <li>mypy strict mode must pass</li> <li>Ruff must pass with no warnings</li> <li>Maintain or improve test coverage (minimum 80%)</li> </ul>"},{"location":"contributing/#commit-messages","title":"Commit Messages","text":"<p>Use Conventional Commits for both commits and PR titles:</p> <pre><code>&lt;type&gt;(&lt;optional-scope&gt;): &lt;short summary&gt;\n</code></pre> <p>Examples:</p> <ul> <li><code>feat: add JSON export support</code></li> <li><code>fix(api): handle empty input</code></li> <li><code>docs: update installation guide</code></li> </ul> <p>Recommended types: <code>feat</code>, <code>fix</code>, <code>docs</code>, <code>refactor</code>, <code>test</code>, <code>ci</code>, <code>chore</code>, <code>perf</code>, <code>build</code>.</p>"},{"location":"contributing/#changelog-policy","title":"Changelog Policy","text":"<p><code>CHANGELOG.md</code> (in Keep a Changelog format) is the canonical, human-curated record of user-facing changes. Add an entry under <code>[Unreleased]</code> for any user-facing change in the same PR that makes it.</p> <p>GitHub's auto-generated release notes (via <code>.github/release.yml</code> categories) are supplementary \u2014 useful for a quick PR-by-PR diff, but <code>CHANGELOG.md</code> is what users should read to understand what changed in a release.</p>"},{"location":"contributing/#getting-help","title":"Getting Help","text":"<p>If something is unclear, open an issue or start a discussion. We're happy to help you get started.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<pre><code>uv tool install mokuji\n</code></pre> <p>Or with pipx / pip:</p> <pre><code>pipx install mokuji\npip install mokuji\n</code></pre> <p>To try it once without installing:</p> <pre><code>uvx mokuji\n</code></pre>"},{"location":"getting-started/#basic-usage","title":"Basic Usage","text":"<p>Run mokuji in a repository:</p> <pre><code>cd your-project\nmokuji\n</code></pre> <p>The first launch opens a short welcome tour \u2014 five pages covering reading, the sidebar, tabs, and search, ending with an offer to open a hands-on tutorial document. Skip it with <code>Esc</code>; replay it any time with <code>?</code> then <code>w</code>.</p> <p>The FILES pane on the left lists the Markdown files under the current directory (press <code>.</code> to show everything else too). Move with <code>j</code>/<code>k</code> and open a document with <code>Enter</code> \u2014 every file opens in its own tab, so <code>gt</code>/<code>gT</code> switch between everything you have opened. Markdown files render with headings, code blocks, and tables; any other text file opens read-only as plain text.</p> <p>A few keys to get productive:</p> <ul> <li><code>t</code> shows the table of contents of the open document; <code>Enter</code> jumps to   a heading.</li> <li><code>/</code> searches within the file (smart case); <code>n</code>/<code>N</code> step through   matches.</li> <li><code>gt</code>/<code>gT</code> switch tabs, <code>x</code> closes one.</li> <li>Click a relative Markdown link to follow it; <code>Ctrl+o</code> goes back.</li> <li><code>?</code> opens the complete key reference at any time.</li> </ul> <p>Tip</p> <p>The footer always shows the keys that work in the focused pane, wrapping onto up to three lines when the terminal is narrow. If you hide it with <code>Ctrl+g</code>, the help modal reminds you how to bring it back.</p>"},{"location":"getting-started/#whats-next","title":"What's Next?","text":"<p>See the Reference for the CLI options and the full keybinding table.</p>"},{"location":"reference/","title":"Reference","text":"<p>mokuji is an application, not a library: its public Python surface is the <code>mokuji</code> command. This page documents the CLI and every keybinding.</p>"},{"location":"reference/#command-line","title":"Command line","text":"Invocation Behavior <code>mokuji</code> Browse the current working directory <code>mokuji &lt;dir&gt;</code> Browse <code>&lt;dir&gt;</code> <code>mokuji &lt;file.md&gt;</code> Browse the file's directory with the file open in a tab <code>mokuji --version</code> Print version and exit <code>mokuji --help</code> Print usage and exit <p><code>python -m mokuji</code> behaves identically.</p> <p>On launch the FILES tree has focus, ready to pick a file with <code>j</code>/<code>k</code> and <code>Enter</code>; when a file is given, the content pane has focus instead.</p>"},{"location":"reference/#exit-codes","title":"Exit codes","text":"Code Meaning <code>0</code> Normal quit <code>1</code> Invalid path (<code>mokuji: path not found: &lt;path&gt;</code> on stderr)"},{"location":"reference/#keybindings","title":"Keybindings","text":""},{"location":"reference/#reading","title":"Reading","text":"Key Action <code>j</code> / <code>k</code> scroll one line <code>d</code> / <code>u</code> half page down / up <code>f</code> / <code>Space</code> / <code>b</code> full page down / down / up <code>gg</code> / <code>G</code> top / bottom <code>r</code> reload file, keeping scroll position"},{"location":"reference/#navigation","title":"Navigation","text":"Key Action <code>e</code> focus the FILES pane, opening it if hidden; press again to hide <code>t</code> focus the TOC pane, opening it if hidden; press again to hide <code>Tab</code> cycle focus tree \u2194 content <code>Enter</code> (tree) open file in a new tab (focus moves to content) / expand directory <code>h</code> / <code>l</code> (tree) collapse / expand directory <code>.</code> (tree) toggle non-Markdown files (hidden by default) <code>/</code> (tree) filter the tree by name (substring, smart case) <code>Enter</code> (TOC) jump to heading <code>Esc</code> (tree) return focus to content <p><code>e</code>/<code>t</code> are focus-or-toggle: a visible-but-unfocused pane gets focused first, and only hides on a second press while it already has focus.</p>"},{"location":"reference/#tabs-history","title":"Tabs &amp; history","text":"Key Action <code>gt</code> / <code>gT</code> next / previous tab <code>1gt</code> \u2026 <code>9gt</code> jump to tab N <code>x</code> close current tab <code>Ctrl+o</code> / <code>Ctrl+i</code> history back / forward (per tab) <p>Tab labels are prefixed with their 1-based index (<code>1 README.md</code>), matching the digit typed for <code>&lt;N&gt;gt</code>; duplicate names still get a parent-directory suffix.</p>"},{"location":"reference/#search","title":"Search","text":"Key Action <code>/</code> open the search input <code>Enter</code> confirm query, jump to the first match <code>n</code> / <code>N</code> next / previous match (wraps with a flash) <code>Esc</code> cancel input / clear highlights <code>S</code> search every Markdown file in the project <p>Queries use smart case: all-lowercase matches case-insensitively; any uppercase letter makes the search case-sensitive.</p> <p>Markdown has no public API for inline highlights, so an in-file match shows as a footer status (<code>match N/M \u00b7 line L \u00b7 &lt;excerpt&gt;</code>) with the matched line's text and the query span accented, instead of an inline highlight. Plain-text files still get full inline highlighting.</p> <p><code>S</code> opens a modal listing every match across the project, grouped by file, capped at 200 hits (20 per file). <code>Up</code>/<code>Down</code> move the selection, <code>Enter</code> opens the selected hit at its line and seeds the in-file search with the same query, and <code>Esc</code> closes the modal with no change. It scans every Markdown file under the project root except <code>.git</code> \u2014 unlike the FILES tree, it ignores the tree's own display filter (dotfiles, build-noise directories).</p>"},{"location":"reference/#meta","title":"Meta","text":"Key Action <code>?</code> help modal (also closes it; <code>Esc</code> too) <code>Ctrl+g</code> toggle the footer key guide <code>q</code> quit"},{"location":"reference/#welcome-tour","title":"Welcome tour","text":"<p>On the very first launch mokuji opens a five-page welcome tour. It is shown once: a marker file under <code>$XDG_STATE_HOME/mokuji/</code> (default <code>~/.local/state/mokuji/</code>) records that it has been seen. To replay it at any time, press <code>?</code> and then <code>w</code>.</p> Key (in the tour) Action <code>l</code> / <code>\u2192</code> next page <code>h</code> / <code>\u2190</code> previous page <code>Enter</code> next page; on the last page, open the hands-on tutorial <code>Esc</code> / <code>q</code> close the tour <p>The last page offers to open a bundled tutorial document \u2014 a short Markdown file designed so every key it teaches can be practised on the document itself.</p>"}]}